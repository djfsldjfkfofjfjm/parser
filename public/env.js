// Этот файл генерируется на сервере и содержит переменные окружения
window.__ENV = {
  FIRECRAWL_API_KEY: '{{FIRECRAWL_API_KEY}}',
  GEMINI_API_KEY: '{{GEMINI_API_KEY}}'
};

// Этот файл будет заменен на серверный env.js при деплое
console.log('Загружен статический env.js (будет заменен на серверный при деплое)');
